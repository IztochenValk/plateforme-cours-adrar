package com.adrar.skillforge;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

//On va importe rla librairi pour générer un UUID et tenter de l'afficher dans la console
//On importe les types
//On appelle l'interface command line runner pour l'utiliser au démarrage

@SpringBootApplication
public class SkillforgeApplication {

public static void main(String[] args) {
		SpringApplication.run(SkillforgeApplication.class, args);
	}
	@Override}
